# 2026-06-28 工作日志

## hcane - DoAutotask 新增格罗姆之花/友好果蝇果催熟功能

在 `modmain.lua` 的 `DoAutotask()` 函数中新增扫描 `glommerflower` / `fruitflyfruit` 的催熟逻辑（插入在高级耕作先驱帽之后、范围催眠之前）：

- 持有格罗姆之花或友好果蝇果时，每 10 秒对 `WORK_RADIUS` 范围内的植物执行催熟
- 催熟类型覆盖：Growable（树木/灌木/农场作物）、Pickable（浆果丛/草根）、Crop（旧版农场作物）、Harvestable（蘑菇农场）
- 同时补充 5x5 地块的营养（每格 12 点三维营养）
- 每次催熟有 36% 概率消耗 1 点饥饿
- 参考了官方 book_silviculture / book_horticulture_upgraded 的 trygrowth 逻辑，以及 3551293930 mod 金苹果的自动催熟思路

## 修复：TendTo 移除

**崩溃：** `farm_plants.lua:111` — `SetPosition` 参数 #2 为 nil

**根因：** `TryGrowth` 的 `crop:DoGrow()` 催熟作物后（可能替换/销毁原实体），紧接着 `TendTo` 调度的延迟 FX 回调（0.5-1.0s后）引用了已失效的 `inst`，导致 `GetWorldPosition()` 返回值不全。

**修复：** 移除 `TendTo` 代码段。营养补充由 `AddTileNutrients` 覆盖，作物生长由 `DoGrow` 直接处理。无 TendTo 后不会再有延迟 FX 回调与失效实体的冲突。

## v2 优化：频率降低 + 过熟保护

**频率降低：** 改为每 10 次自动工作执行一次催熟（≈3.6s），用 `DoAutotask._ripening_counter` 计数，不再依赖 `GetTime()`。

**过熟保护（新增）：**
- 前置跳过 `withered` / `farm_plant_killjoy`（腐烂阶段）标签实体
- 前置跳过 `pickable:CanBePicked()=true`（已可采摘）实体
- 前置跳过 `crop.matured=true`（旧版已成熟）
- farm_plant 的 `domagicgrowthfn` 分支增加 `stage >= #stages` 检查，防止腐烂阶段后循环重生

**根因：** farm_plant 有 `loopstages=true`，DoGrowth 在 stage 5(full) 会推进到 stage 6(rotten→sprout 循环)。官方书籍是单次触发所以影响不大，但自动任务每秒都在调用就会快速过熟。

## v3：奔跑尾迹粒子特效 + 人物跑马灯

### 奔跑尾迹（cane_trail_fx）
- 新 prefab，半透明白色粒子在玩家身后拖尾
- 根据移动速度动态调整发射率（静止 10pps / 奔跑 80pps）
- 修复：`AddRotatingParticleUV` 缺少第 12 个参数 `uv_scale` 导致崩溃

### 人物跑马灯（enable_player_fx）
- 新增布尔配置 `"enable_player_fx"` / `"人物跑马灯"`，默认开启
- `modmain.lua` config 表增加 `enable_player_fx` 读取
- `StartCaneBreathTint`：在手杖跑马灯循环中增加第 3 步——用 `SetAddColour` 对玩家全身施加彩色发光叠加层（强度为手杖的 0.35 倍，效果更淡雅）
- `StopCaneBreathTint`：增加 `SetAddColour(0,0,0,0)` 复位玩家全身
- 与手杖的呼吸/色相同步，同一个 hue/intensity 计算

## v4：配置重构 + 催熟书本 FX

### 配置项调整
- **移除** `enable_player_fx` 布尔配置（人物跑马灯归入呼吸灯控制）
- **多选项** `fx_particle_type`：删除 `trail` 和 `player`，恢复 `sparkle` 默认
- 删除 `cane_trail_fx.lua` Prefab

### 人物跑马灯逻辑
- 人物跑马灯由呼吸灯特效（`enable_light_fx`）统一控制：呼吸灯开启时，**手杖 + 人物全身 + 图标**同时跑马灯
- 移除了 `config.fx_particle_type == "player"` 条件判断

### 催熟 FX
- 由 `fx_plants_big_under_book` 改为 `vault_portal_fx`（传送门特效，更炫酷）
