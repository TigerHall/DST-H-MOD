# 2026-07-05 工作日志 — 行走手杖大规模重构

## ⚠️ 最终状态：周期任务未执行，已还原改前代码

勋章面板显示正常，但所有 FEATURES 的 `task` 周期任务没有启动。
还原后等待下次调整。

---

## 今天所有修改摘要（供后续参考）

### 1. 呼吸灯：白色底光 + 鬼魂着色器（v4.1→v4.4）

- **ItemTile**: 新增 `_cane_glow` 白色呼吸光 Image（后移除）
- **实体**: `SetAddColour` 加白色底光（后移除）
- **核心**: `inst.AnimState:SetHaunted(true)` — 官方鬼魂作祟着色器
  - 开启呼吸灯时调用，关闭时 `SetHaunted(false)`
  - 玩家全身 SetHaunted 跟随 `enable_player_glow` 配置
- **源码参考**: `参考代码/官方源码/components/hauntable.lua`

### 2. 勋章面板兼容（v4.5→v4.6）

- `owner.GetMedalBuffInfo` 回调 → 能力勋章 buff 面板
- `STRINGS.NAMES.HCANE_XXX` 名称映射
- 集中注册，一次性回调监听 `itemget`/`itemlose`

### 3. 全面重构：事件驱动替代轮询（v5.0→v5.1→最终版）

**核心变更**：
- 删除 900 行 `DoAutotask`（原 0.36s 轮询）
- 改为 **CyclicTaskManager** + **FEATURES 注册表** + **OnItemsChanged**

**架构**：
- `StartCyclic(name, interval, fn)` / `StopCyclic(name)` / `StopAllCyclic()`
- `FEATURES` 表：每条定义 `items / apply / reset / task / medal`
- `OnItemsChanged()`：全取消 → 扫描 → 按匹配重新应用
- `ResetAllFeatures(owner, doer)`：卸装/销毁时 STOP ALL + 遍历 reset

**辅助函数**：
- `getPlayer()` — 获取 doer/owner
- `makeCyclic(name, fn)` — 自动检查 `owner._hcane_active` + 自停兜底
- `hasAny(...)` — 替代旧 `HasTargetItem`

**周期任务函数**（各自独立定时器，无对应物品时定时器不存在）：
- OrangeCyclic / RedCyclic / BlueCyclic / OpalCyclic / BeargerCyclic
- DeerclopsCyclic / SkeletonCyclic / MoonrockCyclic / AntlionCyclic
- FarmHatCyclic / AdvFarmHatCyclic / GlommerCyclic / MandrakeCyclic
- AutoSeedCyclic / AutoWeedCyclic / BeefaloCyclic

**FEATURES 条目**（共 24 项，全部带 medal 勋章）：
- 7色宝石 / 彩虹宝石 / 启迪碎片 / 天体珠宝 / 龙鳞
- 麋鹿鹅羽毛 / 毒菌蟾蜍 / 熊大 / 独眼巨鹿 / 远古骨头
- 月球陨石坑 / 蚁狮帽 / 耕作帽(2种) / 格罗姆 / 曼德拉草
- 自动播种(无勋章) / 自动杂草(无勋章) / 牛角牛帽

**关键修复**：
- `StopGlowBreathing` nil 错误 → 函数定义顺序问题（已修复）
- `_original_maxsanity` nil 错误 → OnItemsChanged 开头加初始化
- `table.unpack` → `unpack`（Lua 5.1 兼容）
- `DeerclopsCyclic` 重复获取坐标

**已知问题（当前未修复）**：
- FEATURES 的 `task` 周期任务在装备时未启动，导致各物品的周期工作不执行
- 可能原因：`OnItemsChanged` 执行时机问题，或 `itemget` 事件在装备时未触发
