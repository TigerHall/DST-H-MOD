# 2026-06-19

- 创建了 `参考代码/` 目录，从 DST 游戏 scripts.zip 提取了官方源代码
- 结构：components/ (815个), prefabs/ (1583个), widgets/ (183个), screens/ (63个), 根目录核心脚本/ (21个)
- 加入了 .gitignore 防止官方代码被提交到 Git
- 空 `参考MOD/` 目录留给用户放入其他 MOD 参考代码
- 用户 DST 游戏路径: C:\Program Files (x86)\Steam\steamapps\common\Don't Starve Together
- 行走手杖优化 MOD 新增跑马灯呼吸染色效果（`SetAddColour` + `SetSymbolMultColour` + `SetSymbolAddColour` 三重染色）
- 通过 `mklink /J` 建立 GitHub ↔ DST mods 目录联接
- 修改 modinfo.lua 默认关闭小图标切换（`enable_tool_toggle_icon = false`）
- 创建 MEMORY.md 项目记忆，记录版本管理约定等
