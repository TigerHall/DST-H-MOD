# 2026-07-19 工作日志

## hpaper 模块接线到 Steam
- 将 `制图桌回收/` 通过 Junction 接到 Steam DST mods 目录：`hpaper` → `C:\Users\hehu\Documents\GitHub\DST-H-MOD\制图桌回收`
- 同时更新 `setup-junctions.ps1` 的 `$modMappings` 加入 `"hpaper" = "制图桌回收"`
- 用户已自备 `modicon.tex`/`modicon.xml`；modinfo 走默认图标字段，无需额外配置
- Junction 实测可读：`modinfo.lua`/`modmain.lua`/`modicon.*`/`scripts/components/erasedlibrary.lua` 均可访问
- 用户进入实测阶段（擦除→重制→重载记忆闭环待游戏内验证）

## hpaper modinfo 双语化
- 重写 `制图桌回收/modinfo.lua`：套用用户提供的 `en_zh` / `addConfig` / `addTitle` 辅助函数，全配置项中英双语
- 名称：`en_zh("H-Cartography Desk Enhancement", "H-制图桌强化")`
- 描述动态拼 `V<version>`（`ver_line = "V" .. version .. "\n"`）
- `locale` 全局在 `modinfo.lua` 上下文可用（已用参考 MOD 验证），无需 GLOBAL 包裹
- 修改 modinfo → version 1.0 → 1.1（遵守项目版本管理约定）
- 保留 `icon_atlas`/`icon` 指向 modicon；Junction 即时同步，无需重建

## hpaper 崩溃修复：modmain 加载顺序
- 报错 `modmain.lua:77: attempt to index global 'AllRecipes' (a nil value)`
- 根因：mod 文件夹名 `hpaper` 字母序靠前，modmain 顶层执行时 `require("recipes")` 还没被任何 mod 触发，`AllRecipes`/`Ingredient`/`CRAFTING_FILTERS` 全为 nil
- 修复：modmain 顶部加 `GLOBAL.require("recipes")`（缓存安全，正常路径是空操作，不会清空/重复注册）
- 同时预防 `nounlock` 配方走到 `AddRecipe2` 内部的 `CRAFTING_FILTERS.CRAFTING_STATION` 再崩
- 已把该坑补进 dst-mod-dev 技能 pitfalls.md 第 40 条
- ⚠️ 修正：#40 原 Solution 写 `GLOBAL.require("recipes")` 已实测不足（hpaper 仍崩同一条 AllRecipes），真正可靠修法是 `AddGamePostInit`（见下方 step1 重写）

## hpaper step 1：先让所有草图+蓝图在制图桌可制作（分步验证）
- 用户要求分步：先注册所有蓝图/草图重制配方并立即可在制图桌制作，验证链路；擦除解锁(step2)后做
- 重写 `modmain.lua`：配方注册从顶层循环改到 `AddGamePostInit`（彻底修 AllRecipes 加载顺序坑）
- 关键源码事实（决定写法）：
  - 草图只有**单一 prefab `"sketch"`**（`sketch.lua:174`），种类靠 `sketchid`；`chesspiece_rook_sketch` 只是 craftingstation 的 key 不是 prefab → 配方 `product="sketch"` + builditem 事件补 `sketchid`
  - 制造站菜单靠 `actionstr` 匹配 station 的 `action_str`：制图桌=`"CARTOGRAPHY"`（`recipes.lua:21`）、陶轮=`"SCULPTING"`（`recipes.lua:22`）；官方雕塑配方靠 `actionstr="SCULPTING"` 出现（`recipes.lua:882`）
- 实现要点：
  - `RegisterReissueRecipe(rname, product, image, sketchid)`：`TECH.LOST + nounlock + actionstr="CARTOGRAPHY"`，消耗 `papyrus_cost` 张 papyrus
  - 草图配方 `product="sketch"`，`REISSUE_SKETCH[recipe.name]=sketchid`；blueprint 直接 `product="blueprint"`
  - `AddPlayerPostInit` 监听 `builditem`（仅服务端），prod.prefab=="sketch" 时 `ApplySketchID` 复刻 sketch.lua 的 onload（设 sketchid/名字/图标）
  - 制图桌 `prototyper.onturnon` 预学会全部 `ALL_REISSUE`（step1 全解锁；step2 改为只 LearnItem erasedlibrary 条目）
  - 擦除 hook + erasedlibrary 组件保留不删，step2 接门控用
- 新坑补进 dst-mod-dev pitfalls.md：#41（草图单 prefab 需补 sketchid）、#42（actionstr 必须匹配 station）、#40 修正（AddGamePostInit 优先于 require）
- step1 配方注册改为**顶层 AddRecipe2**（对齐用户成功 mod hsee 写法），加 `GLOBAL.setmetatable(env,{__index=GLOBAL.rawget})`（官方模板标准写法，裸名回退 GLOBAL）
  - 保留 `GLOBAL.require("recipes")` 作为 hpaper 抢跑加载保险（hsee 不需要是因加载晚）；二者不冲突
  - 移除裸 `AllRecipes[rname]` 守卫（原 nil 崩溃源）；注册循环改为顶层 for 循环，不再包 AddGamePostInit
- step1 配方不显示排查：根因是用了 `TECH.LOST`（必须靠 craftingstation 组件 KnowsRecipe 门控，时机没生效）。
  修正为对照官方 mapscroll：`TECH.CARTOGRAPHY_TWO` + `nounlock` + `actionstr="CARTOGRAPHY"`，靠记事桌自身科技显示，删掉整个 craftingstation 组件/onturnon/onturnoff 门控。
  - `TECH.CARTOGRAPHY_TWO={CARTOGRAPHY=2}` 在 constants.lua:1259；书桌 prototyper 树 CARTOGRAPHYDESK={CARTOGRAPHY=2}（tuning.lua:1300）
  - 扩展 OTHER_PAPERS：blueprint/temp_beta_msg(广告)/cookingrecipecard/tacklesketch/scrapbook_page/battlesongs，均用 `GLOBAL.Prefabs[prefab]~=nil` 保护防崩
  - 新坑补 dst-mod-dev pitfalls.md #43（station 配方显示机制：LOST需craftingstation组件，nounlock+自身科技+actionstr 即可显示）

## hpaper 图标/名字修复（step1 收尾）
- 现象：配方出现但无图标、名字泛用（草图全显示"草图"）。
- 根因（源码核实）：菜单名取 `STRINGS.NAMES[recipe.name]`（craftingmenu_details.lua:253），无则退回 product；图标取 `recipe.image` 或 `product..".tex"`（recipetile.lua:89 + recipe.lua:106），图集默认 inventoryimages.xml。原写法配方名 `reissue_xxx` 未注册对应 STRINGS，且草图 image 指 sketch 自己图集的贴图（不在默认图集）致缺失。
- 修法：非草图纸配方名==prefab（krampus 写法，自动复用官方名+贴图）；草图配方 `product="sketch"`（稳定 sketch.tex 图标）+ 注册 `STRINGS.NAMES[REISSUE_SKETCH_n]` = 按官方公式 `subfmt(STRINGS.NAMES.SKETCH,{item=STRINGS.NAMES[大写item.."_builder"]})` 算出的原名（SKETCH 格式 "{item}草图"）。
- 新坑补 dst-mod-dev pitfalls.md #44（配方菜单名取 recipe.name、图标取 product..tex；复用官方名图用配方名==prefab；草图特定贴图在 sketch 自己图集勿给配方 image）。
- modmain 清理重写：顶部大段注释压成几句；SKETCHES 表保留（草图共用单一 prefab "sketch"、靠 sketchid 区分，故该表非冗余——给41种草图各自正确名字+实物图标）；非草图纸用 RegisterPaperRecipe(配方名==prefab 自动复用官方名图)；step2 擦除记录代码明确标注"预留未门控"。
- **重大纠正**：草图不是单一 prefab！官方 sketch.lua:173-186 注册 `Prefab("sketch")` + 每种 `Prefab(item.."_sketch", MakeSketchPrefab(i))`，每个独立 prefab 自带 sketchid/名字/图标（sketch.lua:161-167）。故重制配方可直接 `product="chesspiece_xxx_sketch"` 产出正确草图，**删掉原 ApplySketchID + builditem 的 hack**。
- **蓝图/广告不显示的根因**：modmain 顶层执行时 `Prefabs={}`（main.lua:288 在 LoadMods:392 前重置），原 `if Prefabs[prefab]==nil then return end` 守卫把纸张配方全跳过，草图因无此守卫照常显示。删掉该守卫后全部正常。
- 新坑补 dst-mod-dev pitfalls.md：#44 已纠正"草图单 prefab"误判（实为独立 prefab item.._sketch，直接做产品即可）；新增 #45（modmain 顶层 Prefabs 为空，勿用它守卫跳过配方注册）。

## 动态"擦除→记具体内容→生成配方"模式落地（step2 正式版）
- 用户确认：草图确实是独立 prefab（`item.."_sketch"`）；并提出"mod 每次读列表生成对应制作配方"。改为 erasedlibrary 驱动的动态方案（不再静态注册全部）。
- 架构：cartographydesk DoErase hook（调原逻辑前取内容）→ erasedlibrary:LearnItem(结构化 entry {kind,key}) → 立即 AddRecipe2 生成重制配方；OnLoad 遍历存储重建全部 → "每次读列表生成"。
- 产品映射（对源码核实）：sketch→`item.."_sketch"`（图标统一 image="sketch"）；blueprint→`recipe.."_blueprint"`（blueprint.lua:240 已为每科技配方注册专用蓝图 prefab，精确非随机）；cookingrecipecard→泛用产品 + builditem 事件补 recipe_name/cooker_name（官方无专用 prefab）；temp_beta_msg→`temp_beta_msg` 图标 image="mapscroll"。
- 菜单名：给重制配方注册独立 STRINGS.NAMES[配方名]（subfmt 填 {item}），避免回落到 COOKINGRECIPECARD/TEMP_BETA_MSG 的格式串（{item}食谱卡/{item}广告）。
- 语言取 `GLOBAL.LanguageTranslator.defaultlang`（避免 strict 下裸 locale 报错）。
- 删掉静态 SKETCHES/OTHER_PAPERS 注册循环；modinfo version 1.4→1.5。
- 新坑补 dst-mod-dev pitfalls.md #46（可擦纸是带配置容器，重制产品不能用泛用 prefab；蓝图专用 _blueprint、食谱卡 builditem 补配、菜单名回落格式串）。

## hpaper 收窄支持范围为指定 6 种（v1.6）
- 用户要求：不要全支持，只支持 6 种——蓝图、食谱卡、草图、海盗地图 stash_map、广告(=temp_beta_msg)、礼服款式(yotb 全套蓝图)。
- 确认动态生成完全可行（v1.5 已实现：擦除即时 AddRecipe2 + 重载从 TheWorld 存档重建）。
- 改动：
  - `modmain.lua` DoErase hook 删除通用 `else` 兜底（不再记录任意纸）；新增 `stash`（stash_map 单一固定 prefab，image="stash_map"）和 `costume`（yotb 蓝图，靠 `paper.components.yotb_skinunlocker~=nil` 识别，image="blueprint_sewing_machine_yotb"）。
  - 6 种 entry kind：sketch / blueprint / cooking / stash / ad / costume（+ paper 仅留作旧存档兼容，不再新生成）。
  - `erasedlibrary.lua` entry_eq 增加 stash/costume 的 prefab 比对。
  - modinfo version 1.5→1.6，描述更新为"仅 6 种"。
- 源码核实要点：stash_map 无具体差异（重制=spawn stash_map）；yotb 蓝图 MakeCostumeBlueprint 闭包已写死 skin，spawn 对应 prefab 即得正确礼服；广告=temp_beta_msg（官方 scrapbook 文本"广告/Adverts"即指它，无独立 advert prefab）；所有 STRINGS.NAMES 均已存在。

## hpaper 移除旧存档 paper 兼容
- 用户明确"不需要旧存档兼容"。删除 modmain.lua 中 `kind="paper"` 的 EntryRecipeName/EntryProduct/EntryImage/EntryName 分支，及 erasedlibrary.lua 顶部注释里的 paper 示例与 entry_eq 中的 paper 比对。
- 现 erasedlibrary 仅支持 6 种：sketch/blueprint/cooking/stash/ad/costume。旧存档若含 paper 记录将不再重建（符合用户要求）。modinfo 未改，版本仍 1.6。

## hpaper 回退到静态预注册（v1.7）
- 用户反馈动态方案导致"玩家状态不对"（DoErase hook + erasedlibrary 组件有 runtime 劫持隐患）。改用静态方案：加载时读官方数据表，预注册全部 6 种纸的重制配方。
- 完全移除：DoErase hook、erasedlibrary 组件调用、world AddComponent("erasedlibrary")。
- 删除 `scripts/components/erasedlibrary.lua`（死代码清理）。
- `AddGamePostInit` 中按官方数据表枚举：
  - 草图：遍历 GLOBAL.Prefabs 匹配 `*_sketch` pattern（`key:match("^(.+)_sketch$")`，排除 "sketch"）
  - 蓝图：遍历 AllRecipes，检查 `Prefabs[recipe.name.."_blueprint"]`
  - 食谱卡：`require("cooking").recipe_cards` 迭代（builditem listener 保留用于补配）
  - 海盗地图：Prefabs["stash_map"] 单条
  - 广告：Prefabs["temp_beta_msg"] 单条
  - 礼服款式：`pcall(require, "yotb_costumes")` 迭代 costumes（检查 `v.test~=nil`，即官方注册了 prefab 的）
- 配方格式不变：TECH.CARTOGRAPHY_TWO + nounlock + actionstr="CARTOGRAPHY" + papyrus
- modinfo version 1.6→1.7，描述改为"静态预注册，无运行时劫持"

## hpaper 广告修正为 tacklesketch + 统一图标 + hook 接口（v1.8）
- 用户纠正："广告"不是 temp_beta_msg，而是 tacklesketch（多条 `*_tacklesketch`，结构完全等同 sketch.lua）。删除 temp_beta_msg 段。
- 新增第 5 项 tacklesketch 扫描：pairs(GLOBAL.Prefabs) 匹配 `*_tacklesketch`（排除 "tacklesketch"）。image="tacklesketch" 统一图标；名字用 `subfmt(STRINGS.NAMES.TACKLESKETCH, {item=...})`。
- 6 种顺序调整为：蓝图/食谱卡/草图/海盗地图/广告(tacklesketch)/礼服款式。
- 图标统一：每种类型一个 image 参数（草图="sketch"；蓝图="blueprint"；食谱卡=nil；海盗地图="stash_map"；广告="tacklesketch"；礼服款式="blueprint_sewing_machine_yotb"）。
- 蓝图扫描：用户问 MOD 蓝图是否支持。回答：`AllRecipes` 迭代 + `Prefabs[recipe.."_blueprint"]` 检查，Mod 只要注册了 recipe 和对应 _blueprint prefab 即自动支持。在注释中明确标注。
- 预留擦除解锁接口：modmain 第 31-36 行注释块 `-- local ERASED = {}` / `function IsUnlocked(rname)` 作为钩接点。当前 IsUnlocked 恒 true（全量显示）；后续用户只需在钩中 `MarkErased(rname)` 并修改 `IsUnlocked` 即可变成"只有擦除过的才显示"。
- modinfo version 1.7→1.8，描述更新。

## hpaper 配方图标缺失修复：image 参数缺 .tex 后缀（v1.8→v1.9）
- 用户报告：所有非食谱卡配方无正确图标（空白/无图）。
- 根因追溯（源码级）：
  - `Recipe` 构造函数 `recipe.lua:106`：`self.image = self.imagefn == nil and image or (self.product..".tex")`
  - 当 `image=nil`（食谱卡）→ `self.product..".tex"`（自动加 `.tex`）→ 图集查找成功 ✅
  - 当 `image="blueprint"` → 直接存 `"blueprint"`（无 `.tex`）→ `Image:SetTexture(atlas, "blueprint")` 在 C++ 层查图集时纹理名不匹配（图集条目为 `"blueprint.tex"`）→ 空白 ❌
- 修复：5 种显式 image 值加 `.tex` 后缀：
  - `"blueprint"` → `"blueprint.tex"`
  - `"sketch"` → `"sketch.tex"`
  - `"stash_map"` → `"stash_map.tex"`
  - `"tacklesketch"` → `"tacklesketch.tex"`
  - `"blueprint_sewing_machine_yotb"` → `"blueprint_sewing_machine_yotb.tex"`
  - 食谱卡保持 `image=nil`（自动 `.tex` ✅）
- 修改文件：`制图桌回收/modmain.lua` 第 63/91/97/109/122 行 | `modinfo.lua` 版本 1.8→1.9
- 官方 DST 佐证：所有内置 `Recipe2` 调用 `image` 均带 `.tex`（`"dug_gravestone.tex"`, `"butterfly.tex"`, `"blueprint_rare.tex"` 等）
- 相关坑已补 dst-mod-dev pitfalls.md

## hpaper 去掉 image override，全部走 product 自带图标（v1.9→v1.10）
- 用户要求"都调用自带的图标"→ 移除 `RegisterReissue` 的 `image` 参数，`AddRecipe2` 不再传 `image`
- `recipe.lua:106`：`image=nil` → `self.image = self.product..".tex"`（自动退到 prefab 自带图标）
- 修改：`RegisterReissue` 函数签名从 `(rname, product, image, name_str)` 改为 `(rname, product, name_str)`；6 处调用同步移除 image 参数
- 潜在风险：蓝图的 `xxx_blueprint` 共用 `"blueprint"` 图标（无独立 INV_IMAGE），`"xxx_blueprint.tex"` 可能不存在 → 空白。其他类型（草图/渔具广告的 varaint 有自己的 `v.image`、款式各自有图标）预计正常
- 顶部注释更新为"图标自动取各 product prefab 的自带图标（不传 image）"
- modinfo version 1.9→1.10

## hpaper 最终版：统一图标 + 擦除锁定 + 随机配方（版本不递增）
- 用户反馈"去掉 image"导致部分无图标 → 恢复统一图标方案（显式 `.tex` 传全部 6 种，食谱卡也改成 `"cookingrecipecard.tex"`）
- **新增擦除解锁**：配方注册时 `nounlock=true` + `canbuild`，检查 `TheWorld.components.hmod_carto_erased:IsUnlocked(rname)`。默认全锁定，擦除对应纸张后解锁。
- **新建组件** `scripts/components/hmod_carto_erased.lua`：`OnSave/OnLoad` 持久化解锁记录，全服共享（所有制图桌同状态）。
- **擦除钩子**：`AddComponentPostInit("erasablepaper")` 劫持 `DoErase`，在纸张销毁前通过 `PAPER_TO_RECIPE[paper.prefab]` 逆向查配方名，cookingrecipecard 特例靠 `paper.recipe_name`。
- **新增随机配方**（配置项 `enable_random`/`random_cost` 控制）：
  - 随机蓝图 `hmod_rand_blueprint`：`product="blueprint"`，产出随机未知蓝图（官方 `MakeAnyBlueprint` 机制）
  - 随机食谱卡 `hmod_rand_recipecard`：`product="cookingrecipecard"`，builditem 事件随机从 `cooking.recipe_cards` 抽一个
  - 两者 `nounlock=true`（无需擦除，直接可用），消耗 papyrus 按 `random_cost`
- builditem 监听器扩展为双分支：标准 cooking reissue / random recipecard
- `modinfo.lua` 增加两个配置项 + 描述更新，版本号保持用户原值
- 用户要求：版本号不递增（自行管理）

## hpaper 改为动态注册（擦除时才 AddRecipe2，菜单只显示已解锁）
- 用户反馈三个问题：全部配方灰显可见、随机配方没出现、擦除不解锁。
- **根因**：`nounlock=true` + `canbuild` 只能灰显不能隐藏配方；且擦除钩子逻辑有 bug。
- **方案改为动态注册**：
  - 移除全部预注册循环 + `canbuild`。
  - 擦除钩子 `erasablepaper:DoErase` 中：调用 `GetEraseEntry(paper)` 按类型检测（tag/component/prefab）→ 提取 `{rname, product, image, name_str, cooking?}` → 存入 world 组件 → 立即 `AddRecipe2` → 菜单立即可见。
  - `AddGamePostInit` 遍历 world 组件已存 entries，调用 `RegisterReissueDynamically` 重新注册（`HPAPER` 去重防重注册）。
  - 随机配方保持预注册（`AddGamePostInit` 中 if `enable_random`），无需擦除，始终可见。
- `GetEraseEntry` 检测顺序：sketch tag → tacklesketch tag → blueprint component → yotb_skinunlocker component → stash_map prefab → cookingrecipecard prefab。generic prefab（"sketch"/"blueprint" 无变体）返回 nil，不锁配方。
- 组件 `hmod_carto_erased.lua` 重写：存 `entries`（完整配方数据）+ `cooking`（食谱卡配置），`OnSave/OnLoad` 全部持久化。

## hpaper 改为 KnowsRecipe 钩子方案（预注册 + nounlock=false + RPC 同步）
- 用户要求"类似 UnlockRecipe 给制图桌用" → 改用 KnowsRecipe 钩子
- **方案对比**：动态注册在菜单隐藏机制上受限，且 need client sync。KnowsRecipe 钩子更接近官方蓝图机制（配方预注册→nounlock=false→擦除后 KnowsRecipe 返回 true→显示）
- **核心改动**：
  - 恢复 `AddGamePostInit` 预注册全部 6 种 + 随机配方，全部 `nounlock=false`（默认隐藏）
  - `AddComponentPostInit("builder")` 劫持 `KnowsRecipe`，`reissue_/hmod_` 配方改查世界组件（服务端）或 RPC 缓存（客户端）
  - MOD_RPC 定义 + 客户端 `AddClientModRPCHandler` 处理 `sync_erase` RPC
  - `BroadcastEraseState` 函数：序列化全部已擦除 rname 为逗号分隔串 → `SendModRPCToClient` 广播全员
  - `AddPlayerPostInit` + `DoTaskInTime(1, ...)` 新玩家加入时同步状态
- **擦除钩子**：`PAPER_TO_RECIPE[prefab]` 匹配，cookingrecipecard 特例靠 `recipe_name`，蓝图靠 `teacher+recipetouse`（修复了 `components.blueprint` 不存在的 bug）。写入世界组件后立即 `BroadcastEraseState()`
- **随机配方**保持 `nounlock=true`（直接可见，无需擦除）
- 蓝图检测改用 `paper.components.teacher + paper.recipetouse`（官方蓝图无 `blueprint` 组件）
- 清除了重复的 `BroadcastEraseState`、world 组件、player join handler

## hpaper 改用 builder:UnlockRecipe 全服解锁（最终方案）
- 放弃 KnowsRecipe 钩子 + MOD_RPC 方案（GenerateModRPC 加载顺序坑）
- 改用 DST 内置 `builder:UnlockRecipe(配方名)` → 自动同步到玩家客户端，无需 RPC
- **完整链路**：
  - 预注册全部配方 `nounlock=false`（默认隐藏）
  - 擦除时：检测 `rname` → `world组件:Unlock(rname)` 持久化 → `UnlockForAllPlayers(rname)` 逐个 `builder:UnlockRecipe`
  - 新玩家加入：`DoTaskInTime(1, ...)` 遍历 world 组件全部已解锁 → 逐个 `builder:UnlockRecipe`
  - 随机配方 `nounlock=true` 直接可见
- 删除的代码：MOD_RPC / AddClientModRPCHandler / client_erased / KnowsRecipe 钩子 / BroadcastEraseState / GenerateModRPC
- `hmod_carto_erased.lua` 简化为只存 `unlocked` 集合（不再存完整 entry 数据）
- `modmain.lua` 从 306 行降到 234 行，无 RPC 依赖，无 GenerateModRPC 问题

## hpaper 最终方案 v2：AddClassPostConstruct + UI 过滤
- 用户不接受灰显全部，要求只显示已解锁的
- **方案**：`nounlock=true`（正确标签页 ✅）+ `AddClassPostConstruct("widgets/redux/craftingmenu_widget")` 劫持 `ApplyFilters` → 对 `reissue_/hmod_` 配方只保留 `builder:KnowsRecipe` 返回 true 的
- **数据流**：擦除 → 写世界组件（canbuild 门控）+ `UnlockRecipe`（known_recipes 同步到客户端）→ 客户端 UI 过滤 → 只显示已解锁
- 擦除钩子：检测纸张类型 → 世界组件 `Unlock` + `UnlockForAllPlayers`（known_recipes 同步）
- 玩家加入：`DoTaskInTime(1)` 遍历世界组件逐个 `UnlockRecipe`
- 解决了"正确标签页" + "只显示已解锁" 两个需求

## hpaper 最终方案 v3：TECH.LOST + actionstr + 无 Teacher 钩子
- 用户认可最终逻辑：`TECH.LOST + nounlock=false + actionstr="CARTOGRAPHY"`
- **蓝图阅读（Teacher）不再同步全服**：移除整个 `AddComponentPostInit("teacher")` 钩子
- **擦除 → 同步全服**：写世界组件 + `UnlockForAllPlayers`（依然保持）
- **解锁后配方在制图桌标签页显示**：`actionstr = "CARTOGRAPHY"` 已加回
- 三个核心机制不再冲突：默认隐藏（LOST+nounlock）| 全服共享（擦除同步）| 正确标签页（actionstr）

## hpaper 最终方案 v4：filters 修复 + Teacher 回加
- **`filters` 修复**：`AddRecipe2` 的 `filters` 是第 5 个参数，不是 opts 里的键（官方签名：`AddRecipe2(name, ingredients, tech, opts, filters)`）
- **`TECH.LOST` 确认正确**：重制配方用 `TECH.LOST + nounlock=false + actionstr="CARTOGRAPHY"`，`filters={"PROTOTYPERS","CHARACTER"}`
- **蓝图阅读回加**：读蓝图 → 同时解锁读者本人的 `reissue_blueprint_xxx` + 写入世界组件（仅本人）
- **清理段移除**：`AddPlayerPostInit` 中 `known_recipes` 清理已删除，只剩世界组件同步

## hpaper 最终方案 v3：TECH.LOST + actionstr + 无 Teacher 钩子
- 用户认可最终逻辑：`TECH.LOST + nounlock=false + actionstr="CARTOGRAPHY"`
- **蓝图阅读（Teacher）不再同步全服**：移除整个 `AddComponentPostInit("teacher")` 钩子
- **擦除 → 同步全服**：写世界组件 + `UnlockForAllPlayers`（依然保持）
- **解锁后配方在制图桌标签页显示**：`actionstr = "CARTOGRAPHY"` 已加回
- 三个核心机制不再冲突：默认隐藏（LOST+nounlock）| 全服共享（擦除同步）| 正确标签页（actionstr）
